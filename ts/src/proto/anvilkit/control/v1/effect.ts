// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.3
//   protoc               unknown
// source: anvilkit/control/v1/effect.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
import { messageTypeRegistry } from "../../../typeRegistry.js";
import { CommandIdentity } from "./control.js";
import { ExecutionBinding } from "./dispatch.js";

export const protobufPackage = "anvilkit.control.v1";

/**
 * Guarded business-write obligations (DD-02 §5 business-write class, DD-06 §3):
 * inventory before permission, one single-use permit per mutation bound to its
 * registered physical owner, idempotent outcome observations and the
 * original-result query of a lost response.
 *
 * Only the request that consumes the permit receives permitted=true. Querying
 * an effect, lost responses, lease expiry, retries and restarts never return a
 * second permit; an unknown outcome keeps the original identity until an
 * observation, an original-identity query or an evidence-bound operator
 * disposition (RecoveryService.Dispose) resolves it.
 */

export enum EffectKind {
  EFFECT_KIND_UNSPECIFIED = 0,
  EFFECT_KIND_BUSINESS_WRITE = 1,
  EFFECT_KIND_PUBLICATION = 2,
  EFFECT_KIND_ACTIVATION = 3,
  EFFECT_KIND_REVIEW = 4,
  UNRECOGNIZED = -1,
}

export function effectKindFromJSON(object: any): EffectKind {
  switch (object) {
    case 0:
    case "EFFECT_KIND_UNSPECIFIED":
      return EffectKind.EFFECT_KIND_UNSPECIFIED;
    case 1:
    case "EFFECT_KIND_BUSINESS_WRITE":
      return EffectKind.EFFECT_KIND_BUSINESS_WRITE;
    case 2:
    case "EFFECT_KIND_PUBLICATION":
      return EffectKind.EFFECT_KIND_PUBLICATION;
    case 3:
    case "EFFECT_KIND_ACTIVATION":
      return EffectKind.EFFECT_KIND_ACTIVATION;
    case 4:
    case "EFFECT_KIND_REVIEW":
      return EffectKind.EFFECT_KIND_REVIEW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EffectKind.UNRECOGNIZED;
  }
}

export function effectKindToJSON(object: EffectKind): string {
  switch (object) {
    case EffectKind.EFFECT_KIND_UNSPECIFIED:
      return "EFFECT_KIND_UNSPECIFIED";
    case EffectKind.EFFECT_KIND_BUSINESS_WRITE:
      return "EFFECT_KIND_BUSINESS_WRITE";
    case EffectKind.EFFECT_KIND_PUBLICATION:
      return "EFFECT_KIND_PUBLICATION";
    case EffectKind.EFFECT_KIND_ACTIVATION:
      return "EFFECT_KIND_ACTIVATION";
    case EffectKind.EFFECT_KIND_REVIEW:
      return "EFFECT_KIND_REVIEW";
    case EffectKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EffectState {
  EFFECT_STATE_UNSPECIFIED = 0,
  EFFECT_STATE_PREPARED = 1,
  EFFECT_STATE_PERMITTED = 2,
  EFFECT_STATE_SUCCEEDED = 3,
  EFFECT_STATE_FAILED = 4,
  EFFECT_STATE_UNKNOWN = 5,
  EFFECT_STATE_CONFIRMED_NOT_SENT = 6,
  EFFECT_STATE_DENIED = 7,
  UNRECOGNIZED = -1,
}

export function effectStateFromJSON(object: any): EffectState {
  switch (object) {
    case 0:
    case "EFFECT_STATE_UNSPECIFIED":
      return EffectState.EFFECT_STATE_UNSPECIFIED;
    case 1:
    case "EFFECT_STATE_PREPARED":
      return EffectState.EFFECT_STATE_PREPARED;
    case 2:
    case "EFFECT_STATE_PERMITTED":
      return EffectState.EFFECT_STATE_PERMITTED;
    case 3:
    case "EFFECT_STATE_SUCCEEDED":
      return EffectState.EFFECT_STATE_SUCCEEDED;
    case 4:
    case "EFFECT_STATE_FAILED":
      return EffectState.EFFECT_STATE_FAILED;
    case 5:
    case "EFFECT_STATE_UNKNOWN":
      return EffectState.EFFECT_STATE_UNKNOWN;
    case 6:
    case "EFFECT_STATE_CONFIRMED_NOT_SENT":
      return EffectState.EFFECT_STATE_CONFIRMED_NOT_SENT;
    case 7:
    case "EFFECT_STATE_DENIED":
      return EffectState.EFFECT_STATE_DENIED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EffectState.UNRECOGNIZED;
  }
}

export function effectStateToJSON(object: EffectState): string {
  switch (object) {
    case EffectState.EFFECT_STATE_UNSPECIFIED:
      return "EFFECT_STATE_UNSPECIFIED";
    case EffectState.EFFECT_STATE_PREPARED:
      return "EFFECT_STATE_PREPARED";
    case EffectState.EFFECT_STATE_PERMITTED:
      return "EFFECT_STATE_PERMITTED";
    case EffectState.EFFECT_STATE_SUCCEEDED:
      return "EFFECT_STATE_SUCCEEDED";
    case EffectState.EFFECT_STATE_FAILED:
      return "EFFECT_STATE_FAILED";
    case EffectState.EFFECT_STATE_UNKNOWN:
      return "EFFECT_STATE_UNKNOWN";
    case EffectState.EFFECT_STATE_CONFIRMED_NOT_SENT:
      return "EFFECT_STATE_CONFIRMED_NOT_SENT";
    case EffectState.EFFECT_STATE_DENIED:
      return "EFFECT_STATE_DENIED";
    case EffectState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EffectOutcome {
  EFFECT_OUTCOME_UNSPECIFIED = 0,
  EFFECT_OUTCOME_SUCCEEDED = 1,
  EFFECT_OUTCOME_FAILED = 2,
  EFFECT_OUTCOME_UNKNOWN = 3,
  UNRECOGNIZED = -1,
}

export function effectOutcomeFromJSON(object: any): EffectOutcome {
  switch (object) {
    case 0:
    case "EFFECT_OUTCOME_UNSPECIFIED":
      return EffectOutcome.EFFECT_OUTCOME_UNSPECIFIED;
    case 1:
    case "EFFECT_OUTCOME_SUCCEEDED":
      return EffectOutcome.EFFECT_OUTCOME_SUCCEEDED;
    case 2:
    case "EFFECT_OUTCOME_FAILED":
      return EffectOutcome.EFFECT_OUTCOME_FAILED;
    case 3:
    case "EFFECT_OUTCOME_UNKNOWN":
      return EffectOutcome.EFFECT_OUTCOME_UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EffectOutcome.UNRECOGNIZED;
  }
}

export function effectOutcomeToJSON(object: EffectOutcome): string {
  switch (object) {
    case EffectOutcome.EFFECT_OUTCOME_UNSPECIFIED:
      return "EFFECT_OUTCOME_UNSPECIFIED";
    case EffectOutcome.EFFECT_OUTCOME_SUCCEEDED:
      return "EFFECT_OUTCOME_SUCCEEDED";
    case EffectOutcome.EFFECT_OUTCOME_FAILED:
      return "EFFECT_OUTCOME_FAILED";
    case EffectOutcome.EFFECT_OUTCOME_UNKNOWN:
      return "EFFECT_OUTCOME_UNKNOWN";
    case EffectOutcome.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Effect {
  $type: "anvilkit.control.v1.Effect";
  effectId: string;
  operationId: string;
  attemptId: string;
  kind: EffectKind;
  occurrence: string;
  owner: string;
  canonicalSubject: string;
  expectedRevision?: string | undefined;
  state: EffectState;
  outcome: EffectOutcome;
  denialCode?: string | undefined;
  outcomeRef?: string | undefined;
  executionEpoch: string;
  recoveryEpoch: string;
  deadline: Date | undefined;
  createdAt: Date | undefined;
  observedAt?: Date | undefined;
}

/**
 * Lease binds the mutation to the caller's lease on the upstream subject
 * (DD-06 §1 LeasePort): a stable lease identity, its fence and its expiry.
 */
export interface Lease {
  $type: "anvilkit.control.v1.Lease";
  leaseId: string;
  fence: string;
  expiresAt: Date | undefined;
}

export interface PrepareEffectRequest {
  $type: "anvilkit.control.v1.PrepareEffectRequest";
  command: CommandIdentity | undefined;
  binding: ExecutionBinding | undefined;
  owner: string;
  kind: EffectKind;
  occurrence: string;
  canonicalSubject: string;
  expectedRevision?: string | undefined;
  deadline: Date | undefined;
  lease?: Lease | undefined;
}

export interface PrepareEffectResponse {
  $type: "anvilkit.control.v1.PrepareEffectResponse";
  effect: Effect | undefined;
  permitted: boolean;
  denialCode?: string | undefined;
}

export interface ObserveEffectRequest {
  $type: "anvilkit.control.v1.ObserveEffectRequest";
  tenantId: string;
  effectId: string;
  source: string;
  sequence: string;
  outcome: EffectOutcome;
  /** receipt_digest names the upstream receipt of a definite outcome. */
  receiptDigest: string;
  nativeReference: string;
  observedAt: Date | undefined;
}

export interface ObserveEffectResponse {
  $type: "anvilkit.control.v1.ObserveEffectResponse";
  effect: Effect | undefined;
  existing: boolean;
}

export interface GetEffectRequest {
  $type: "anvilkit.control.v1.GetEffectRequest";
  tenantId: string;
  effectId: string;
  operationId: string;
  kind: EffectKind;
  occurrence: string;
}

export interface GetEffectResponse {
  $type: "anvilkit.control.v1.GetEffectResponse";
  effect: Effect | undefined;
}

export interface QueryEffectOutcomeRequest {
  $type: "anvilkit.control.v1.QueryEffectOutcomeRequest";
  tenantId: string;
  effectId: string;
}

export interface QueryEffectOutcomeResponse {
  $type: "anvilkit.control.v1.QueryEffectOutcomeResponse";
  effect:
    | Effect
    | undefined;
  /** known is true when the upstream had a record of the original identity. */
  known: boolean;
}

function createBaseEffect(): Effect {
  return {
    $type: "anvilkit.control.v1.Effect",
    effectId: "",
    operationId: "",
    attemptId: "",
    kind: 0,
    occurrence: "",
    owner: "",
    canonicalSubject: "",
    expectedRevision: undefined,
    state: 0,
    outcome: 0,
    denialCode: undefined,
    outcomeRef: undefined,
    executionEpoch: "",
    recoveryEpoch: "",
    deadline: undefined,
    createdAt: undefined,
    observedAt: undefined,
  };
}

export const Effect: MessageFns<Effect, "anvilkit.control.v1.Effect"> = {
  $type: "anvilkit.control.v1.Effect" as const,

  encode(message: Effect, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.effectId !== "") {
      writer.uint32(10).string(message.effectId);
    }
    if (message.operationId !== "") {
      writer.uint32(18).string(message.operationId);
    }
    if (message.attemptId !== "") {
      writer.uint32(26).string(message.attemptId);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.occurrence !== "") {
      writer.uint32(42).string(message.occurrence);
    }
    if (message.owner !== "") {
      writer.uint32(50).string(message.owner);
    }
    if (message.canonicalSubject !== "") {
      writer.uint32(58).string(message.canonicalSubject);
    }
    if (message.expectedRevision !== undefined) {
      writer.uint32(66).string(message.expectedRevision);
    }
    if (message.state !== 0) {
      writer.uint32(72).int32(message.state);
    }
    if (message.outcome !== 0) {
      writer.uint32(80).int32(message.outcome);
    }
    if (message.denialCode !== undefined) {
      writer.uint32(90).string(message.denialCode);
    }
    if (message.outcomeRef !== undefined) {
      writer.uint32(98).string(message.outcomeRef);
    }
    if (message.executionEpoch !== "") {
      writer.uint32(106).string(message.executionEpoch);
    }
    if (message.recoveryEpoch !== "") {
      writer.uint32(114).string(message.recoveryEpoch);
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(122).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(130).fork()).join();
    }
    if (message.observedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.observedAt), writer.uint32(138).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Effect {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEffect();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.effectId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.operationId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.attemptId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.occurrence = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.owner = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.canonicalSubject = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.expectedRevision = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.state = reader.int32() as any;
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.outcome = reader.int32() as any;
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.denialCode = reader.string();
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.outcomeRef = reader.string();
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.executionEpoch = reader.string();
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.recoveryEpoch = reader.string();
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 17: {
            if (tag !== 138) {
              break;
            }

            message.observedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Effect {
    return {
      $type: Effect.$type,
      effectId: isSet(object.effectId)
        ? globalThis.String(object.effectId)
        : isSet(object.effect_id)
        ? globalThis.String(object.effect_id)
        : "",
      operationId: isSet(object.operationId)
        ? globalThis.String(object.operationId)
        : isSet(object.operation_id)
        ? globalThis.String(object.operation_id)
        : "",
      attemptId: isSet(object.attemptId)
        ? globalThis.String(object.attemptId)
        : isSet(object.attempt_id)
        ? globalThis.String(object.attempt_id)
        : "",
      kind: isSet(object.kind) ? effectKindFromJSON(object.kind) : 0,
      occurrence: isSet(object.occurrence) ? globalThis.String(object.occurrence) : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      canonicalSubject: isSet(object.canonicalSubject)
        ? globalThis.String(object.canonicalSubject)
        : isSet(object.canonical_subject)
        ? globalThis.String(object.canonical_subject)
        : "",
      expectedRevision: isSet(object.expectedRevision)
        ? globalThis.String(object.expectedRevision)
        : isSet(object.expected_revision)
        ? globalThis.String(object.expected_revision)
        : undefined,
      state: isSet(object.state) ? effectStateFromJSON(object.state) : 0,
      outcome: isSet(object.outcome) ? effectOutcomeFromJSON(object.outcome) : 0,
      denialCode: isSet(object.denialCode)
        ? globalThis.String(object.denialCode)
        : isSet(object.denial_code)
        ? globalThis.String(object.denial_code)
        : undefined,
      outcomeRef: isSet(object.outcomeRef)
        ? globalThis.String(object.outcomeRef)
        : isSet(object.outcome_ref)
        ? globalThis.String(object.outcome_ref)
        : undefined,
      executionEpoch: isSet(object.executionEpoch)
        ? globalThis.String(object.executionEpoch)
        : isSet(object.execution_epoch)
        ? globalThis.String(object.execution_epoch)
        : "",
      recoveryEpoch: isSet(object.recoveryEpoch)
        ? globalThis.String(object.recoveryEpoch)
        : isSet(object.recovery_epoch)
        ? globalThis.String(object.recovery_epoch)
        : "",
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      observedAt: isSet(object.observedAt)
        ? fromJsonTimestamp(object.observedAt)
        : isSet(object.observed_at)
        ? fromJsonTimestamp(object.observed_at)
        : undefined,
    };
  },

  toJSON(message: Effect): unknown {
    const obj: any = {};
    if (message.effectId !== "") {
      obj.effectId = message.effectId;
    }
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.attemptId !== "") {
      obj.attemptId = message.attemptId;
    }
    if (message.kind !== 0) {
      obj.kind = effectKindToJSON(message.kind);
    }
    if (message.occurrence !== "") {
      obj.occurrence = message.occurrence;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.canonicalSubject !== "") {
      obj.canonicalSubject = message.canonicalSubject;
    }
    if (message.expectedRevision !== undefined) {
      obj.expectedRevision = message.expectedRevision;
    }
    if (message.state !== 0) {
      obj.state = effectStateToJSON(message.state);
    }
    if (message.outcome !== 0) {
      obj.outcome = effectOutcomeToJSON(message.outcome);
    }
    if (message.denialCode !== undefined) {
      obj.denialCode = message.denialCode;
    }
    if (message.outcomeRef !== undefined) {
      obj.outcomeRef = message.outcomeRef;
    }
    if (message.executionEpoch !== "") {
      obj.executionEpoch = message.executionEpoch;
    }
    if (message.recoveryEpoch !== "") {
      obj.recoveryEpoch = message.recoveryEpoch;
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.observedAt !== undefined) {
      obj.observedAt = message.observedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Effect>, I>>(base?: I): Effect {
    return Effect.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Effect>, I>>(object: I): Effect {
    const message = createBaseEffect();
    message.effectId = object.effectId ?? "";
    message.operationId = object.operationId ?? "";
    message.attemptId = object.attemptId ?? "";
    message.kind = object.kind ?? 0;
    message.occurrence = object.occurrence ?? "";
    message.owner = object.owner ?? "";
    message.canonicalSubject = object.canonicalSubject ?? "";
    message.expectedRevision = object.expectedRevision ?? undefined;
    message.state = object.state ?? 0;
    message.outcome = object.outcome ?? 0;
    message.denialCode = object.denialCode ?? undefined;
    message.outcomeRef = object.outcomeRef ?? undefined;
    message.executionEpoch = object.executionEpoch ?? "";
    message.recoveryEpoch = object.recoveryEpoch ?? "";
    message.deadline = object.deadline ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.observedAt = object.observedAt ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Effect.$type, Effect);

function createBaseLease(): Lease {
  return { $type: "anvilkit.control.v1.Lease", leaseId: "", fence: "", expiresAt: undefined };
}

export const Lease: MessageFns<Lease, "anvilkit.control.v1.Lease"> = {
  $type: "anvilkit.control.v1.Lease" as const,

  encode(message: Lease, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.fence !== "") {
      writer.uint32(18).string(message.fence);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lease {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLease();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.leaseId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.fence = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Lease {
    return {
      $type: Lease.$type,
      leaseId: isSet(object.leaseId)
        ? globalThis.String(object.leaseId)
        : isSet(object.lease_id)
        ? globalThis.String(object.lease_id)
        : "",
      fence: isSet(object.fence) ? globalThis.String(object.fence) : "",
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: Lease): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.fence !== "") {
      obj.fence = message.fence;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lease>, I>>(base?: I): Lease {
    return Lease.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lease>, I>>(object: I): Lease {
    const message = createBaseLease();
    message.leaseId = object.leaseId ?? "";
    message.fence = object.fence ?? "";
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Lease.$type, Lease);

function createBasePrepareEffectRequest(): PrepareEffectRequest {
  return {
    $type: "anvilkit.control.v1.PrepareEffectRequest",
    command: undefined,
    binding: undefined,
    owner: "",
    kind: 0,
    occurrence: "",
    canonicalSubject: "",
    expectedRevision: undefined,
    deadline: undefined,
    lease: undefined,
  };
}

export const PrepareEffectRequest: MessageFns<PrepareEffectRequest, "anvilkit.control.v1.PrepareEffectRequest"> = {
  $type: "anvilkit.control.v1.PrepareEffectRequest" as const,

  encode(message: PrepareEffectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== undefined) {
      CommandIdentity.encode(message.command, writer.uint32(10).fork()).join();
    }
    if (message.binding !== undefined) {
      ExecutionBinding.encode(message.binding, writer.uint32(18).fork()).join();
    }
    if (message.owner !== "") {
      writer.uint32(26).string(message.owner);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.occurrence !== "") {
      writer.uint32(42).string(message.occurrence);
    }
    if (message.canonicalSubject !== "") {
      writer.uint32(50).string(message.canonicalSubject);
    }
    if (message.expectedRevision !== undefined) {
      writer.uint32(58).string(message.expectedRevision);
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(66).fork()).join();
    }
    if (message.lease !== undefined) {
      Lease.encode(message.lease, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareEffectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePrepareEffectRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.command = CommandIdentity.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.binding = ExecutionBinding.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.owner = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.occurrence = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.canonicalSubject = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.expectedRevision = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.lease = Lease.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PrepareEffectRequest {
    return {
      $type: PrepareEffectRequest.$type,
      command: isSet(object.command) ? CommandIdentity.fromJSON(object.command) : undefined,
      binding: isSet(object.binding) ? ExecutionBinding.fromJSON(object.binding) : undefined,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      kind: isSet(object.kind) ? effectKindFromJSON(object.kind) : 0,
      occurrence: isSet(object.occurrence) ? globalThis.String(object.occurrence) : "",
      canonicalSubject: isSet(object.canonicalSubject)
        ? globalThis.String(object.canonicalSubject)
        : isSet(object.canonical_subject)
        ? globalThis.String(object.canonical_subject)
        : "",
      expectedRevision: isSet(object.expectedRevision)
        ? globalThis.String(object.expectedRevision)
        : isSet(object.expected_revision)
        ? globalThis.String(object.expected_revision)
        : undefined,
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
      lease: isSet(object.lease) ? Lease.fromJSON(object.lease) : undefined,
    };
  },

  toJSON(message: PrepareEffectRequest): unknown {
    const obj: any = {};
    if (message.command !== undefined) {
      obj.command = CommandIdentity.toJSON(message.command);
    }
    if (message.binding !== undefined) {
      obj.binding = ExecutionBinding.toJSON(message.binding);
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.kind !== 0) {
      obj.kind = effectKindToJSON(message.kind);
    }
    if (message.occurrence !== "") {
      obj.occurrence = message.occurrence;
    }
    if (message.canonicalSubject !== "") {
      obj.canonicalSubject = message.canonicalSubject;
    }
    if (message.expectedRevision !== undefined) {
      obj.expectedRevision = message.expectedRevision;
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    if (message.lease !== undefined) {
      obj.lease = Lease.toJSON(message.lease);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrepareEffectRequest>, I>>(base?: I): PrepareEffectRequest {
    return PrepareEffectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrepareEffectRequest>, I>>(object: I): PrepareEffectRequest {
    const message = createBasePrepareEffectRequest();
    message.command = (object.command !== undefined && object.command !== null)
      ? CommandIdentity.fromPartial(object.command)
      : undefined;
    message.binding = (object.binding !== undefined && object.binding !== null)
      ? ExecutionBinding.fromPartial(object.binding)
      : undefined;
    message.owner = object.owner ?? "";
    message.kind = object.kind ?? 0;
    message.occurrence = object.occurrence ?? "";
    message.canonicalSubject = object.canonicalSubject ?? "";
    message.expectedRevision = object.expectedRevision ?? undefined;
    message.deadline = object.deadline ?? undefined;
    message.lease = (object.lease !== undefined && object.lease !== null) ? Lease.fromPartial(object.lease) : undefined;
    return message;
  },
};

messageTypeRegistry.set(PrepareEffectRequest.$type, PrepareEffectRequest);

function createBasePrepareEffectResponse(): PrepareEffectResponse {
  return {
    $type: "anvilkit.control.v1.PrepareEffectResponse",
    effect: undefined,
    permitted: false,
    denialCode: undefined,
  };
}

export const PrepareEffectResponse: MessageFns<PrepareEffectResponse, "anvilkit.control.v1.PrepareEffectResponse"> = {
  $type: "anvilkit.control.v1.PrepareEffectResponse" as const,

  encode(message: PrepareEffectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.effect !== undefined) {
      Effect.encode(message.effect, writer.uint32(10).fork()).join();
    }
    if (message.permitted !== false) {
      writer.uint32(16).bool(message.permitted);
    }
    if (message.denialCode !== undefined) {
      writer.uint32(26).string(message.denialCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareEffectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePrepareEffectResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.effect = Effect.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.permitted = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.denialCode = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PrepareEffectResponse {
    return {
      $type: PrepareEffectResponse.$type,
      effect: isSet(object.effect) ? Effect.fromJSON(object.effect) : undefined,
      permitted: isSet(object.permitted) ? globalThis.Boolean(object.permitted) : false,
      denialCode: isSet(object.denialCode)
        ? globalThis.String(object.denialCode)
        : isSet(object.denial_code)
        ? globalThis.String(object.denial_code)
        : undefined,
    };
  },

  toJSON(message: PrepareEffectResponse): unknown {
    const obj: any = {};
    if (message.effect !== undefined) {
      obj.effect = Effect.toJSON(message.effect);
    }
    if (message.permitted !== false) {
      obj.permitted = message.permitted;
    }
    if (message.denialCode !== undefined) {
      obj.denialCode = message.denialCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrepareEffectResponse>, I>>(base?: I): PrepareEffectResponse {
    return PrepareEffectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrepareEffectResponse>, I>>(object: I): PrepareEffectResponse {
    const message = createBasePrepareEffectResponse();
    message.effect = (object.effect !== undefined && object.effect !== null)
      ? Effect.fromPartial(object.effect)
      : undefined;
    message.permitted = object.permitted ?? false;
    message.denialCode = object.denialCode ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(PrepareEffectResponse.$type, PrepareEffectResponse);

function createBaseObserveEffectRequest(): ObserveEffectRequest {
  return {
    $type: "anvilkit.control.v1.ObserveEffectRequest",
    tenantId: "",
    effectId: "",
    source: "",
    sequence: "",
    outcome: 0,
    receiptDigest: "",
    nativeReference: "",
    observedAt: undefined,
  };
}

export const ObserveEffectRequest: MessageFns<ObserveEffectRequest, "anvilkit.control.v1.ObserveEffectRequest"> = {
  $type: "anvilkit.control.v1.ObserveEffectRequest" as const,

  encode(message: ObserveEffectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.effectId !== "") {
      writer.uint32(18).string(message.effectId);
    }
    if (message.source !== "") {
      writer.uint32(26).string(message.source);
    }
    if (message.sequence !== "") {
      writer.uint32(34).string(message.sequence);
    }
    if (message.outcome !== 0) {
      writer.uint32(40).int32(message.outcome);
    }
    if (message.receiptDigest !== "") {
      writer.uint32(50).string(message.receiptDigest);
    }
    if (message.nativeReference !== "") {
      writer.uint32(58).string(message.nativeReference);
    }
    if (message.observedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.observedAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObserveEffectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseObserveEffectRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.tenantId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.effectId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.source = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.sequence = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.outcome = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.receiptDigest = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.nativeReference = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.observedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ObserveEffectRequest {
    return {
      $type: ObserveEffectRequest.$type,
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      effectId: isSet(object.effectId)
        ? globalThis.String(object.effectId)
        : isSet(object.effect_id)
        ? globalThis.String(object.effect_id)
        : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      sequence: isSet(object.sequence) ? globalThis.String(object.sequence) : "",
      outcome: isSet(object.outcome) ? effectOutcomeFromJSON(object.outcome) : 0,
      receiptDigest: isSet(object.receiptDigest)
        ? globalThis.String(object.receiptDigest)
        : isSet(object.receipt_digest)
        ? globalThis.String(object.receipt_digest)
        : "",
      nativeReference: isSet(object.nativeReference)
        ? globalThis.String(object.nativeReference)
        : isSet(object.native_reference)
        ? globalThis.String(object.native_reference)
        : "",
      observedAt: isSet(object.observedAt)
        ? fromJsonTimestamp(object.observedAt)
        : isSet(object.observed_at)
        ? fromJsonTimestamp(object.observed_at)
        : undefined,
    };
  },

  toJSON(message: ObserveEffectRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.effectId !== "") {
      obj.effectId = message.effectId;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.sequence !== "") {
      obj.sequence = message.sequence;
    }
    if (message.outcome !== 0) {
      obj.outcome = effectOutcomeToJSON(message.outcome);
    }
    if (message.receiptDigest !== "") {
      obj.receiptDigest = message.receiptDigest;
    }
    if (message.nativeReference !== "") {
      obj.nativeReference = message.nativeReference;
    }
    if (message.observedAt !== undefined) {
      obj.observedAt = message.observedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObserveEffectRequest>, I>>(base?: I): ObserveEffectRequest {
    return ObserveEffectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObserveEffectRequest>, I>>(object: I): ObserveEffectRequest {
    const message = createBaseObserveEffectRequest();
    message.tenantId = object.tenantId ?? "";
    message.effectId = object.effectId ?? "";
    message.source = object.source ?? "";
    message.sequence = object.sequence ?? "";
    message.outcome = object.outcome ?? 0;
    message.receiptDigest = object.receiptDigest ?? "";
    message.nativeReference = object.nativeReference ?? "";
    message.observedAt = object.observedAt ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ObserveEffectRequest.$type, ObserveEffectRequest);

function createBaseObserveEffectResponse(): ObserveEffectResponse {
  return { $type: "anvilkit.control.v1.ObserveEffectResponse", effect: undefined, existing: false };
}

export const ObserveEffectResponse: MessageFns<ObserveEffectResponse, "anvilkit.control.v1.ObserveEffectResponse"> = {
  $type: "anvilkit.control.v1.ObserveEffectResponse" as const,

  encode(message: ObserveEffectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.effect !== undefined) {
      Effect.encode(message.effect, writer.uint32(10).fork()).join();
    }
    if (message.existing !== false) {
      writer.uint32(16).bool(message.existing);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObserveEffectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseObserveEffectResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.effect = Effect.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.existing = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ObserveEffectResponse {
    return {
      $type: ObserveEffectResponse.$type,
      effect: isSet(object.effect) ? Effect.fromJSON(object.effect) : undefined,
      existing: isSet(object.existing) ? globalThis.Boolean(object.existing) : false,
    };
  },

  toJSON(message: ObserveEffectResponse): unknown {
    const obj: any = {};
    if (message.effect !== undefined) {
      obj.effect = Effect.toJSON(message.effect);
    }
    if (message.existing !== false) {
      obj.existing = message.existing;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObserveEffectResponse>, I>>(base?: I): ObserveEffectResponse {
    return ObserveEffectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObserveEffectResponse>, I>>(object: I): ObserveEffectResponse {
    const message = createBaseObserveEffectResponse();
    message.effect = (object.effect !== undefined && object.effect !== null)
      ? Effect.fromPartial(object.effect)
      : undefined;
    message.existing = object.existing ?? false;
    return message;
  },
};

messageTypeRegistry.set(ObserveEffectResponse.$type, ObserveEffectResponse);

function createBaseGetEffectRequest(): GetEffectRequest {
  return {
    $type: "anvilkit.control.v1.GetEffectRequest",
    tenantId: "",
    effectId: "",
    operationId: "",
    kind: 0,
    occurrence: "",
  };
}

export const GetEffectRequest: MessageFns<GetEffectRequest, "anvilkit.control.v1.GetEffectRequest"> = {
  $type: "anvilkit.control.v1.GetEffectRequest" as const,

  encode(message: GetEffectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.effectId !== "") {
      writer.uint32(18).string(message.effectId);
    }
    if (message.operationId !== "") {
      writer.uint32(26).string(message.operationId);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.occurrence !== "") {
      writer.uint32(42).string(message.occurrence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEffectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetEffectRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.tenantId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.effectId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.operationId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.occurrence = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetEffectRequest {
    return {
      $type: GetEffectRequest.$type,
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      effectId: isSet(object.effectId)
        ? globalThis.String(object.effectId)
        : isSet(object.effect_id)
        ? globalThis.String(object.effect_id)
        : "",
      operationId: isSet(object.operationId)
        ? globalThis.String(object.operationId)
        : isSet(object.operation_id)
        ? globalThis.String(object.operation_id)
        : "",
      kind: isSet(object.kind) ? effectKindFromJSON(object.kind) : 0,
      occurrence: isSet(object.occurrence) ? globalThis.String(object.occurrence) : "",
    };
  },

  toJSON(message: GetEffectRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.effectId !== "") {
      obj.effectId = message.effectId;
    }
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.kind !== 0) {
      obj.kind = effectKindToJSON(message.kind);
    }
    if (message.occurrence !== "") {
      obj.occurrence = message.occurrence;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEffectRequest>, I>>(base?: I): GetEffectRequest {
    return GetEffectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEffectRequest>, I>>(object: I): GetEffectRequest {
    const message = createBaseGetEffectRequest();
    message.tenantId = object.tenantId ?? "";
    message.effectId = object.effectId ?? "";
    message.operationId = object.operationId ?? "";
    message.kind = object.kind ?? 0;
    message.occurrence = object.occurrence ?? "";
    return message;
  },
};

messageTypeRegistry.set(GetEffectRequest.$type, GetEffectRequest);

function createBaseGetEffectResponse(): GetEffectResponse {
  return { $type: "anvilkit.control.v1.GetEffectResponse", effect: undefined };
}

export const GetEffectResponse: MessageFns<GetEffectResponse, "anvilkit.control.v1.GetEffectResponse"> = {
  $type: "anvilkit.control.v1.GetEffectResponse" as const,

  encode(message: GetEffectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.effect !== undefined) {
      Effect.encode(message.effect, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEffectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetEffectResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.effect = Effect.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetEffectResponse {
    return {
      $type: GetEffectResponse.$type,
      effect: isSet(object.effect) ? Effect.fromJSON(object.effect) : undefined,
    };
  },

  toJSON(message: GetEffectResponse): unknown {
    const obj: any = {};
    if (message.effect !== undefined) {
      obj.effect = Effect.toJSON(message.effect);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEffectResponse>, I>>(base?: I): GetEffectResponse {
    return GetEffectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEffectResponse>, I>>(object: I): GetEffectResponse {
    const message = createBaseGetEffectResponse();
    message.effect = (object.effect !== undefined && object.effect !== null)
      ? Effect.fromPartial(object.effect)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(GetEffectResponse.$type, GetEffectResponse);

function createBaseQueryEffectOutcomeRequest(): QueryEffectOutcomeRequest {
  return { $type: "anvilkit.control.v1.QueryEffectOutcomeRequest", tenantId: "", effectId: "" };
}

export const QueryEffectOutcomeRequest: MessageFns<
  QueryEffectOutcomeRequest,
  "anvilkit.control.v1.QueryEffectOutcomeRequest"
> = {
  $type: "anvilkit.control.v1.QueryEffectOutcomeRequest" as const,

  encode(message: QueryEffectOutcomeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.effectId !== "") {
      writer.uint32(18).string(message.effectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEffectOutcomeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryEffectOutcomeRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.tenantId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.effectId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryEffectOutcomeRequest {
    return {
      $type: QueryEffectOutcomeRequest.$type,
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      effectId: isSet(object.effectId)
        ? globalThis.String(object.effectId)
        : isSet(object.effect_id)
        ? globalThis.String(object.effect_id)
        : "",
    };
  },

  toJSON(message: QueryEffectOutcomeRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.effectId !== "") {
      obj.effectId = message.effectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryEffectOutcomeRequest>, I>>(base?: I): QueryEffectOutcomeRequest {
    return QueryEffectOutcomeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryEffectOutcomeRequest>, I>>(object: I): QueryEffectOutcomeRequest {
    const message = createBaseQueryEffectOutcomeRequest();
    message.tenantId = object.tenantId ?? "";
    message.effectId = object.effectId ?? "";
    return message;
  },
};

messageTypeRegistry.set(QueryEffectOutcomeRequest.$type, QueryEffectOutcomeRequest);

function createBaseQueryEffectOutcomeResponse(): QueryEffectOutcomeResponse {
  return { $type: "anvilkit.control.v1.QueryEffectOutcomeResponse", effect: undefined, known: false };
}

export const QueryEffectOutcomeResponse: MessageFns<
  QueryEffectOutcomeResponse,
  "anvilkit.control.v1.QueryEffectOutcomeResponse"
> = {
  $type: "anvilkit.control.v1.QueryEffectOutcomeResponse" as const,

  encode(message: QueryEffectOutcomeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.effect !== undefined) {
      Effect.encode(message.effect, writer.uint32(10).fork()).join();
    }
    if (message.known !== false) {
      writer.uint32(16).bool(message.known);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEffectOutcomeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryEffectOutcomeResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.effect = Effect.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.known = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryEffectOutcomeResponse {
    return {
      $type: QueryEffectOutcomeResponse.$type,
      effect: isSet(object.effect) ? Effect.fromJSON(object.effect) : undefined,
      known: isSet(object.known) ? globalThis.Boolean(object.known) : false,
    };
  },

  toJSON(message: QueryEffectOutcomeResponse): unknown {
    const obj: any = {};
    if (message.effect !== undefined) {
      obj.effect = Effect.toJSON(message.effect);
    }
    if (message.known !== false) {
      obj.known = message.known;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryEffectOutcomeResponse>, I>>(base?: I): QueryEffectOutcomeResponse {
    return QueryEffectOutcomeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryEffectOutcomeResponse>, I>>(object: I): QueryEffectOutcomeResponse {
    const message = createBaseQueryEffectOutcomeResponse();
    message.effect = (object.effect !== undefined && object.effect !== null)
      ? Effect.fromPartial(object.effect)
      : undefined;
    message.known = object.known ?? false;
    return message;
  },
};

messageTypeRegistry.set(QueryEffectOutcomeResponse.$type, QueryEffectOutcomeResponse);

export type EffectServiceService = typeof EffectServiceService;
export const EffectServiceService = {
  /**
   * Prepares the business-write obligation (inventory first) and consumes the
   * single permit; the same command returns the existing effect with
   * permitted=false. A closed recovery scope, a fenced operation, a passed
   * deadline or an expired lease is recorded as denied.
   */
  prepareEffect: {
    path: "/anvilkit.control.v1.EffectService/PrepareEffect" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PrepareEffectRequest): Buffer => Buffer.from(PrepareEffectRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PrepareEffectRequest => PrepareEffectRequest.decode(value),
    responseSerialize: (value: PrepareEffectResponse): Buffer =>
      Buffer.from(PrepareEffectResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PrepareEffectResponse => PrepareEffectResponse.decode(value),
  },
  /**
   * Idempotent outcome observation; a lost report is resubmitted under the
   * same source sequence. A definite outcome needs a receipt reference.
   */
  observeEffect: {
    path: "/anvilkit.control.v1.EffectService/ObserveEffect" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ObserveEffectRequest): Buffer => Buffer.from(ObserveEffectRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ObserveEffectRequest => ObserveEffectRequest.decode(value),
    responseSerialize: (value: ObserveEffectResponse): Buffer =>
      Buffer.from(ObserveEffectResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ObserveEffectResponse => ObserveEffectResponse.decode(value),
  },
  /** Original-result query by effect id or by (operation, kind, occurrence). */
  getEffect: {
    path: "/anvilkit.control.v1.EffectService/GetEffect" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetEffectRequest): Buffer => Buffer.from(GetEffectRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetEffectRequest => GetEffectRequest.decode(value),
    responseSerialize: (value: GetEffectResponse): Buffer => Buffer.from(GetEffectResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetEffectResponse => GetEffectResponse.decode(value),
  },
  /**
   * Asks the upstream about the original identity and records the answer as
   * an observation; an upstream without a record leaves the effect unknown.
   */
  queryEffectOutcome: {
    path: "/anvilkit.control.v1.EffectService/QueryEffectOutcome" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: QueryEffectOutcomeRequest): Buffer =>
      Buffer.from(QueryEffectOutcomeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): QueryEffectOutcomeRequest => QueryEffectOutcomeRequest.decode(value),
    responseSerialize: (value: QueryEffectOutcomeResponse): Buffer =>
      Buffer.from(QueryEffectOutcomeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): QueryEffectOutcomeResponse => QueryEffectOutcomeResponse.decode(value),
  },
} as const;

export interface EffectServiceServer extends UntypedServiceImplementation {
  /**
   * Prepares the business-write obligation (inventory first) and consumes the
   * single permit; the same command returns the existing effect with
   * permitted=false. A closed recovery scope, a fenced operation, a passed
   * deadline or an expired lease is recorded as denied.
   */
  prepareEffect: handleUnaryCall<PrepareEffectRequest, PrepareEffectResponse>;
  /**
   * Idempotent outcome observation; a lost report is resubmitted under the
   * same source sequence. A definite outcome needs a receipt reference.
   */
  observeEffect: handleUnaryCall<ObserveEffectRequest, ObserveEffectResponse>;
  /** Original-result query by effect id or by (operation, kind, occurrence). */
  getEffect: handleUnaryCall<GetEffectRequest, GetEffectResponse>;
  /**
   * Asks the upstream about the original identity and records the answer as
   * an observation; an upstream without a record leaves the effect unknown.
   */
  queryEffectOutcome: handleUnaryCall<QueryEffectOutcomeRequest, QueryEffectOutcomeResponse>;
}

export interface EffectServiceClient extends Client {
  /**
   * Prepares the business-write obligation (inventory first) and consumes the
   * single permit; the same command returns the existing effect with
   * permitted=false. A closed recovery scope, a fenced operation, a passed
   * deadline or an expired lease is recorded as denied.
   */
  prepareEffect(
    request: PrepareEffectRequest,
    callback: (error: ServiceError | null, response: PrepareEffectResponse) => void,
  ): ClientUnaryCall;
  prepareEffect(
    request: PrepareEffectRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PrepareEffectResponse) => void,
  ): ClientUnaryCall;
  prepareEffect(
    request: PrepareEffectRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PrepareEffectResponse) => void,
  ): ClientUnaryCall;
  /**
   * Idempotent outcome observation; a lost report is resubmitted under the
   * same source sequence. A definite outcome needs a receipt reference.
   */
  observeEffect(
    request: ObserveEffectRequest,
    callback: (error: ServiceError | null, response: ObserveEffectResponse) => void,
  ): ClientUnaryCall;
  observeEffect(
    request: ObserveEffectRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ObserveEffectResponse) => void,
  ): ClientUnaryCall;
  observeEffect(
    request: ObserveEffectRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ObserveEffectResponse) => void,
  ): ClientUnaryCall;
  /** Original-result query by effect id or by (operation, kind, occurrence). */
  getEffect(
    request: GetEffectRequest,
    callback: (error: ServiceError | null, response: GetEffectResponse) => void,
  ): ClientUnaryCall;
  getEffect(
    request: GetEffectRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetEffectResponse) => void,
  ): ClientUnaryCall;
  getEffect(
    request: GetEffectRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetEffectResponse) => void,
  ): ClientUnaryCall;
  /**
   * Asks the upstream about the original identity and records the answer as
   * an observation; an upstream without a record leaves the effect unknown.
   */
  queryEffectOutcome(
    request: QueryEffectOutcomeRequest,
    callback: (error: ServiceError | null, response: QueryEffectOutcomeResponse) => void,
  ): ClientUnaryCall;
  queryEffectOutcome(
    request: QueryEffectOutcomeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: QueryEffectOutcomeResponse) => void,
  ): ClientUnaryCall;
  queryEffectOutcome(
    request: QueryEffectOutcomeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: QueryEffectOutcomeResponse) => void,
  ): ClientUnaryCall;
}

export const EffectServiceClient = makeGenericClientConstructor(
  EffectServiceService,
  "anvilkit.control.v1.EffectService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): EffectServiceClient;
  service: typeof EffectServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { $type: "google.protobuf.Timestamp", seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
